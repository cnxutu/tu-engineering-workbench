# 仓库 AI 上下文入口

本仓库接入共享的 `ai-guidance` 知识库。

## 必须的读取顺序

启动时按以下顺序读取：

1. 通过 `AI_GUIDANCE_HOME` 或仓库显式配置定位 P0 的 `ai-guidance`。
2. 读取 P0 的运行时入口以及当前仓库和目标目录就近的 `AGENTS.md`，先确认公共与局部约束。
3. 读取仓库清单，确定当前仓库绑定的产品。
4. 在产品知识中优先读取绑定产品的 `index.md`，确认范围、证据状态和后续按需读取路径。
5. 只读取任务所需的 Core 角色、规则、工作流或 Template，再核实 `README` 和当前代码。

采用渐进式读取：从产品入口开始，只增加完成下一步判断所必需的文档；不要预加载无关产品材料。

## 优先级与事实

语义优先级独立于读取顺序。平台、系统和开发者约束优先于用户要求；其后依次为用户最新明确要求、仓库局部约束、P0/Core 公共规则、产品上下文与示例。当前代码、契约、配置和测试用于核实当前事实，不取代用户已授权的目标变更。

只有能取得且适用的证据才可将产品事实视为已验证。需求、问题或评审若建立或修改了跨服务事实（服务职责、API/消息契约、部署拓扑或端到端链路），必须评估是否更新产品知识；记录证据并更新对应文档，或明确说明为何无需更新。

禁止将密钥、Token、凭据、客户数据或其他敏感运行信息写入知识、任务记录或证据摘录。
