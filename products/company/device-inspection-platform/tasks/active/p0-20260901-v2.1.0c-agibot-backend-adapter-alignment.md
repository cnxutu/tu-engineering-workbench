# 2.1.0.c 智元机器狗：后端与协议适配对接方案

> 状态：待与协议适配负责人冻结  
> 更新日期：2026-09-01  
> 对应链路说明：[巡检业务平台—物联网平台链路兼容关注点](p0-20260901-v2.1.0c-agibot-interface-ledger-and-p1-p2-chain.md)  
> 物模型依据：本次提供的 `productKey = robot-dog` 物模型文件；下述“待确认”项不是已确认实现。

## 1. 先说清楚：同一个 Topic 如何识别机器狗

`iot_business_event:DEFAULT_FLOW` 是巡检业务平台消费的**运输通道**，其中会混入其他产品的上行消息；不能、也不需要通过 Topic 区分机器狗。

机器狗需要经过两层识别，职责不可混淆：

| 层次 | 识别依据 | 用途 | 结论 |
|---|---|---|---|
| 消息路由 | `params.identifier` | 决定交给哪个消息 Handler | 必须由物联网平台/适配器按约定写入；P1 Router 只认该值。 |
| 设备类型校验 | `message.deviceId` 查询到的平台设备分类 `categoryCode`（机器狗分类） | 决定该 Handler 是否按机器狗业务处理及绑定项目 | 以平台设备主数据为准；上报的 `productKey=robot-dog` 只作一致性校验和日志，不作为授权或唯一可信来源。 |

因此，机器狗不需要独立 RocketMQ Topic。巡检业务平台需要新增“机器狗设备上下文解析”：通过 `deviceId`/`deviceSn` 定位独立机器狗设备，再校验其 `categoryCode` 是双方冻结的机器狗分类。不能复用当前 `InspectionDeviceContextService` 的机场—无人机父子设备假设，也不能把机器狗当作机场遥测处理。

### 1.1 当前代码中“设备类型分流”的实际入口

当前分支的设备类型不是从 RocketMQ Topic 或物模型 payload 的 `productKey` 直接判断，而是从物联网平台设备主数据取回 `categoryCode`。顺着下面链路即可直观看到机器狗要插入的位置：

```text
P2 上行消息 message.deviceId
  ↓
P1 设备查询：IDeviceService#getDeviceById
  ↓
ManagedDeviceQueryService#getDeviceById
  ↓
IotDeviceQueryGateway#getDeviceById
  ↓
IoTDeviceApi#get(deviceId)  —— 从物联网平台取得 IotDeviceDTO
  ↓
IotDeviceQueryGateway#mapToDeviceDTO
  └─ rpcDevice.categoryCode → DeviceDTO.categoryCode
  ↓
RobotDogDeviceContextService#resolve（新增）
  └─ 校验 categoryCode == 机器狗 P2 分类编码
```

对应代码入口与当前事实：

| 目的 | 代码入口 | 当前事实 | 机器狗需要改什么 |
|---|---|---|---|
| 从消息取设备主键 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/convert/InspectionIotMessageConverter.java:148` 的 `toInspectionMessage` | 原始消息的 `deviceId` 被写入 `InspectionIotMessage.deviceId`。 | 同事上行消息必须带真实的物联网平台机器狗 `deviceId`，不能只带 SN。 |
| 由设备 ID 查询主数据 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/device/ManagedDeviceQueryService.java:62` 的 `getDeviceById` | 通过 `IotDeviceQueryGateway` 查询物联网平台设备。 | 机器狗 Handler 优先从这里取得独立设备，而不是沿用机场/子机关系。 |
| 物联网平台 DTO 映射为业务设备 DTO | `b-inspection-platform-common/src/main/java/com/xmkj/business/common/device/rpc/IotDeviceQueryGateway.java:49` 的 `getDeviceById`；`:263` 的 `mapToDeviceDTO` | 当前明确执行 `rpcDevice.getCategoryCode() → DeviceDTO.categoryCode`。`productKey` 被映射到 `modelCode`，不是当前类型分流主字段。 | 同事需给出机器狗的 P2 `categoryCode`；P1 在机器狗上下文服务中校验这个字段。 |
| 监控展示分类映射 | `b-inspection-platform-common/src/main/java/com/xmkj/business/common/enums/dict/DictMonitorDeviceCategoryEnum.java:10`；`:60` 的 `fromP2CategoryCode` | `ROBOT_DOG` 枚举当前仍被注释，尚无 `p2CategoryCode` 映射。 | 启用 `ROBOT_DOG("机器狗", "ROBOT_DOG", "<双方冻结的P2分类编码>")`，让监控列表/投影识别机器狗。 |
| 设备投影的分类过滤 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/device/DeviceProjectionSyncTask.java:165` 的 `mapCategory`；`DeviceProjectionRealtimeSyncService.java:144` | 依赖上述枚举；未映射的 P2 分类会被视为未知并跳过。 | 先完成枚举映射，再做机器狗投影/列表，否则监控页查询不到设备。 |
| 当前机场/无人机上下文 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionDeviceContextServiceImpl.java:28` 的 `resolve` | 优先按 `dockSn`、父设备、子设备解析，内部变量和分支均是 dock/drone。 | 不在此类里叠加机器狗 if；新建 `RobotDogDeviceContextService#resolve(message.deviceId, payload.deviceSn)`，只接受机器狗独立设备。 |

建议新增的机器狗上下文校验伪代码如下，便于你和同事确认责任边界：

```java
DeviceDTO device = deviceService.getDeviceById(message.getDeviceId())
    .orElseThrow(/* 机器狗设备不存在 */);

if (!ROBOT_DOG_P2_CATEGORY.equals(device.getCategoryCode())) {
    throw /* 消息 identifier 是机器狗，但设备分类不匹配 */;
}
if (!device.getDeviceSn().equals(payload.getDeviceSn())) {
    throw /* deviceId / deviceSn 不一致 */;
}
return device; // 后续用其 deviceId、项目/空间归属、权限上下文
```

其中 `ROBOT_DOG_P2_CATEGORY` 的具体值不能由巡检业务平台猜测，必须由同事在 B-01 中给出并写入物联网平台分类初始化 SQL。这个校验就是你要跟进的“按设备类型分流”最小代码入口。

## 2. 当前物模型已确认的事实

| 类别 | 当前物模型定义 | 对 P1 的含义 |
|---|---|---|
| 产品 | `productKey = robot-dog` | 物联网平台需给该产品/设备配置机器狗分类；巡检业务平台按分类分流。 |
| 属性 | 有 `identifier`、`reportType`、`deviceSn`、`productKey`，以及电池、模式、急停、补光、速度、里程、控制源、运控等平铺属性 | 适合作为机器狗 OSD/状态快照来源；应由专用 `RobotDogStatusProperty` 承接，不能绑定现有 DJI `DeviceStatusProperty`。 |
| 事件 | `inspection_alarm_report`、`inspection_control_notification_report` | 名称与当前 DJI 通用 Handler 相同，但当前事件 payload 和业务处理不兼容；见第 4 节，必须选择兼容方案。 |
| 服务 | `inspection_device_control`，`callType=async`；参数含 `command`、`lx`、`ly`、`rx`、`ry`、`bodyTurn`、`bodyHighLow` | 可复用 P1→物联网平台的服务调用主链路；P1 需新增机器狗命令 DTO/门面。异步返回不代表设备动作完成。 |
| 配置服务 | `inspection_config_command`，`callType=async` | 本版本若做传感器频率或摄像头码率控制，需要新增机器狗配置 DTO；不做则不接入 P1 页面。 |
| 坐标 | 仅有 `motionPositionX/Y/Z` 等本地运控位置 | **没有经纬度**。当前前端已确认需要最近经纬度地图点位，坐标转换责任与契约必须补齐，见第 6 节。 |

## 3. 推荐上行契约与路径图

### 3.1 状态属性：建议独立的机器狗路由 identifier

推荐固定状态路由值为 `inspection_robot_dog_status_report`。它是 P1 Router 的路由键；当前物模型中名为 `identifier` 的属性应上报这个**值**。该值并不是把 `iot_business_event` 再分一个 Topic。

状态消息应使用当前 P1 Converter 已支持的属性上报形态：

```json
{
  "id": "...",
  "requestId": "...",
  "deviceId": 123,
  "method": "thing.property.post",
  "reportTime": "...",
  "params": {
    "identifier": "inspection_robot_dog_status_report",
    "reportType": "STATE",
    "deviceSn": "...",
    "productKey": "robot-dog",
    "robotMode": "general",
    "batteryPower1": 88.1,
    "softwareEmergencyStop": false
  }
}
```

P1 Converter 对 `thing.property.post` 会以 `params.identifier` 作为路由键，并把其余平铺字段组装为 Handler payload。因此 P1 的实现应为：

```text
inspection_robot_dog_status_report
  → RobotDogStatusPropertyHandler
  → 按 deviceId / deviceSn 解析独立机器狗设备并校验产品分类
  → 更新机器狗状态快照、最近位置、业务状态
  → 推送机器狗 OSD / businessStatus / 监控统计变化
```

`reportType` 可作为状态分包的内部字段，但不得代替 `params.identifier` 做 P1 路由。高频 IMU、JointState、运控原始数据不得默认全部经 P2 Data Rule 转给 P1；应由适配器/物联网平台按页面实际需要采样，只将 OSD、告警、控制权等业务数据投递 P1。

### 3.2 故障、控制权、命令回执：不要和 DJI payload 混用

当前物模型的两个事件 identifier 与现有 P1 Handler 同名，但当前 P1 行为如下：

- `inspection_alarm_report`：现有 Handler 解析 `AlarmReport.items`，HMS Service 又按 DJI `dock_tip_` / `fpv_tip_` 字典组装文案。
- `inspection_control_notification_report`：现有 Handler 只接受 `DRC_STATUS_NOTIFY`、`JOYSTICK_INVALID_NOTIFY`，并按机场上下文推送。

机器狗的 `faults[]`、`CONTROL_TAKEN` / `CONTROL_RELEASED` 不能直接进入这两条实现。P1 Router 同一个 identifier 只能注册一个 Handler，因此不能额外挂一个同名机器狗 Handler。

推荐在物模型发布前改为独立 identifier：

| 能力 | 当前物模型 | 推荐冻结值 | P1 落点 |
|---|---|---|---|
| 机器狗状态属性 | `identifier` 属性值尚未冻结 | `inspection_robot_dog_status_report` | 新 `RobotDogStatusPropertyHandler` |
| 机器狗故障事件 | `inspection_alarm_report` | `inspection_robot_dog_alarm_report` | 新 `RobotDogAlarmReportHandler` + 机器狗 HMS 映射 |
| 机器狗控制权通知 | `inspection_control_notification_report` | `inspection_robot_dog_control_notification_report` | 新 `RobotDogControlNotificationHandler` |
| 机器狗异步命令结果 | 当前物模型未定义 | `inspection_robot_dog_command_reply` | 新 `RobotDogCommandReplyHandler`，更新命令流水/WS |

如同事坚持复用现有两条事件 identifier，则他必须把 payload 完整适配为现有 `AlarmReport.items` 和 `ControlNotificationReport`，并由 P1 改造现有业务服务按设备分类分流。该方案会碰到 DJI HMS 文案、机场上下文和前端消息语义，风险与改动量更高；不建议作为首选。

### 3.3 上行全链路

```text
智元 SDK 回调 / 协议 1004、1005、1016、1017
  ↓  同事负责：字段转换、连接生命周期、设备身份
协议适配器 / 物联网网关
  ↓  IotDeviceMessage（deviceId、method、params.identifier、params/payload）
物联网平台
  ├─ 校验机器狗产品物模型、保存属性/运行态
  └─ Data Rule：仅将约定的机器狗状态、故障、控制权、命令回执转发
       ↓  RocketMQ iot_business_event:DEFAULT_FLOW
巡检业务平台 Consumer → Converter → identifier Router
  ↓
机器狗专用 Handler（状态 / 故障 / 控制权 / 命令回执）
  ↓
状态快照、最近位置、HMS、控制权会话、业务状态
  ↓
REST 首屏兜底 + WebSocket（OSD、businessStatus、告警、控制权、命令结果）
```

## 4. 下行对接方案

当前物模型已定义 `inspection_device_control`，这与 P1 当前 `InspectionIotCommandService#invokeDeviceControl` 使用的服务 identifier 一致。因此**下行传输主链路可复用**，但不能直接用现有 DJI DTO。

```text
前端机器狗页面
  ↓  机器狗 Controller / Service（P1 新增或改造）
设备分类、项目权限、机器狗控制权、命令白名单、审计、幂等
  ↓
RobotDogControlCommand（P1 新 DTO）
  ↓  identifier = inspection_device_control
IoTDeviceApi.invokeDeviceService
  ↓  校验机器狗产品的服务 TSL、设备 gateway 归属
物联网网关 / 协议适配器
  ↓  同事负责：1002 低频命令 / 1003 遥控报文 / SDK 调用
智元机器狗
  ↓
状态变化 + 命令回执事件 → 回到上行链路
```

### 4.1 P1 与同事的分工

| 范围 | 巡检业务平台（本人） | 物联网平台、网关、协议适配器、设备（同事） |
|---|---|---|
| 设备识别 | 用 `deviceId + deviceSn + 产品分类` 解析机器狗上下文，绑定项目与权限 | 创建/导入机器狗产品与设备，提供设备 ID、SN、产品分类、网关归属映射。 |
| 上行 | 新 DTO、Handler、快照、最近位置、HMS、WebSocket、前端 REST | 将 SDK 消息映射为冻结的 `method + identifier + payload`，配 P2 Data Rule，提供样本。 |
| 低频控制 | 命令白名单、控制权、审计、调用 `inspection_device_control`、前端文案 | 将 `command` 映射到协议 1002，给出 ACK/失败码/状态回报。 |
| 手动遥控 | 用户会话、权限、前端按键状态、断开时释放控制权 | 接收目标控制量，并在持有 SDK 连接的一侧以 50Hz 下发；超时必须自动归零。 |
| 急停 | 权限、操作审计、互斥校验、前端显示 | 映射急停/解除急停命令；上报软/硬急停真实状态。 |
| 异步结果 | 命令状态流水、WS 结果提示 | 每个异步服务回传 `requestId`、`command`、阶段、code/message、设备时间。 |

### 4.2 下行服务与命令冻结表

| 服务 | 当前物模型 | P1 处理建议 | 必须向同事确认 |
|---|---|---|---|
| 低频控制 | `inspection_device_control`，异步 | 新 `RobotDogControlCommand`，复用同一服务 identifier；不要扩写 DJI `DeviceControlCommand` 的飞行字段。 | 每个 `command` 到 1002/1003 的映射、参数是否必填、允许模式、设备 ACK 及错误码。 |
| 手动遥控 | `REMOTE_CONTROL + lx/ly/rx/ry/bodyTurn/bodyHighLow` | P1 只发送按键状态/目标向量及会话信息；不让 P1 HTTP 以 50Hz 轮询。 | 适配器 50Hz 定时、控制量 TTL、失联/浏览器断开归零、控制源切换、并发控制。 |
| 急停/解除急停 | `EMERGENCY_STOP` / `EMERGENCY_RECOVER`（物模型枚举） | 单独的机器狗接口和权限点；前端命令互斥；完成由真实状态确认。 | 软/硬急停区别、是否可解除硬急停、解除前置、指令 ACK/失败与状态上报。 |
| 模式/步态/速度/补光/充电 | 已列入 `command` 枚举 | P1 白名单校验后按异步命令发送。 | 每个枚举值是否真实支持 D1 Max、命令完成以哪一个 OSD 字段证明。 |
| 配置 | `inspection_config_command`，异步 | 非本期页面能力则暂不实现 P1 门面。 | 是否进入当前版本、摄像头码率/传感器频率回执方式。 |

## 5. 巡检业务平台代码入口地图

以下路径均基于 `c-drone-inspection` 的 `develop/2.1.0` 分支。可以按“入口 → 现有参考 → 机器狗改造点”阅读；除明确标注“新增”外，不表示要改动所有列出的文件。

### 5.1 上行消费、identifier 与机器狗状态

| 关注点 | 当前代码入口 | 当前行为 | 机器狗改造落点 |
|---|---|---|---|
| RocketMQ 消费入口 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/consumer/InspectionIotUpstreamConsumer.java:62` 的 `handleMessage` | 消费 `iot_business_event:DEFAULT_FLOW`，执行 `converter.convertAll(...).forEach(router::route)`。 | **不改消费 Topic/Consumer**；先由同事提供可消费的原始 JSON。 |
| 从原消息取 identifier | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/convert/InspectionIotMessageConverter.java:48` 的 `convertAll` | 读取 `params.identifier`；若 `method=thing.property.post`，把除 `identifier` 外的平铺字段作为 payload。 | 同事状态报文必须设置 `params.identifier=inspection_robot_dog_status_report`；机器狗状态若也用标准属性上报，通常不需要改 Converter。 |
| identifier 到 Handler 的唯一映射 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/router/InspectionIotMessageRouter.java:25`、`:46` | Spring 注入的 Handler 在启动时按 `identifier()` 建 Map；未知 identifier 记录错误后确认消费；重复 identifier 启动直接失败。 | 在 `InspectionIotIdentifiers.java` 新增机器狗 identifier 常量，并新增各自唯一的 Handler；**不能**为现有 identifier 再注册第二个 Handler。 |
| 现有状态 Handler 参考 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/handler/DeviceStatusPropertyHandler.java:16`、`:38` | 将 `inspection_device_status_report` 绑定为 DJI `DeviceStatusProperty` 并转交状态业务服务。 | **新增** `RobotDogStatusProperty`、`RobotDogStatusPropertyHandler`、机器狗状态业务服务；不要把机器狗字段加进该 DJI DTO。 |
| 设备上下文解析 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionDeviceContextServiceImpl.java:28` 的 `resolve` | 按机场/无人机父子关系解析上下文，有“机场”“飞机”占位兼容分支。 | **新增**独立 `RobotDogDeviceContextService`：用 `message.deviceId` 查询设备，再核验 `deviceSn`、产品分类和项目归属；不走机场父子分支。 |
| 状态快照、在线与业务状态的参考 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionDeviceStatusBusinessServiceImpl.java:161` 的 `handle`；`:252` / `:319` 的状态归约与 OSD 推送 | 当前按机场/无人机分流、写 Redis、推 OSD，并调用 `MonitorBusinessStatusNotifier`。 | **新增**机器狗快照/最近位置/独立 `businessStatus` 归约；可复用 Redis、Notifier、WebSocket 调用方式，但状态规则和 DTO 独立。 |
| 常量位置 | `b-inspection-platform-common/src/main/java/com/xmkj/business/common/iot/model/InspectionIotIdentifiers.java:9` | 集中维护 P1 识别的上行/下行 identifier。 | 将冻结的机器狗状态、告警、控制权、命令回执 identifier 加入这里，保证 P1/同事/物联网平台同一份名称清单。 |

建议的最小新增文件集合（命名为建议，评审后可调整）：

```text
b-inspection-platform-common
  └─ iot/model/upstream/RobotDogStatusProperty.java
  └─ iot/model/upstream/RobotDogAlarmReport.java
  └─ iot/model/upstream/RobotDogControlNotificationReport.java
  └─ iot/model/downstream/RobotDogControlCommand.java

b-inspection-platform-iot
  └─ handler/RobotDogStatusPropertyHandler.java
  └─ handler/RobotDogAlarmReportHandler.java
  └─ handler/RobotDogControlNotificationHandler.java
  └─ handler/RobotDogCommandReplyHandler.java

b-inspection-platform-core
  └─ service/iot/impl/RobotDogDeviceContextServiceImpl.java
  └─ service/iot/impl/RobotDogStatusBusinessServiceImpl.java
  └─ service/iot/impl/RobotDogAlarmBusinessServiceImpl.java
  └─ service/iot/impl/RobotDogControlBusinessServiceImpl.java
```

### 5.2 故障、控制权与 WebSocket 的参考入口

| 场景 | 当前代码入口 | 为什么不能直接复用 | 机器狗实现跟进点 |
|---|---|---|---|
| HMS 告警 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/handler/AlarmReportHandler.java:16`；`b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionAlarmBusinessServiceImpl.java:63` | 现有 Service 用 DJI `dock_tip_` / `fpv_tip_` HMS 字典组装告警文案。 | 机器狗故障 Handler/Service 独立；可复用 HMS 表、分页入口和 30 秒合并技术实现，但需定义机器狗故障 key、恢复和文案映射。 |
| 控制权通知 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/handler/ControlNotificationReportHandler.java:16`；`b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionControlNotificationBusinessServiceImpl.java:37` | 仅接受 DJI 的 `DRC_STATUS_NOTIFY`、`JOYSTICK_INVALID_NOTIFY`，且按机场上下文推送。 | 机器狗 `CONTROL_TAKEN/CONTROL_RELEASED` 进入独立 Handler，更新机器狗控制权会话后推送页面。 |
| 异步命令回执 | `b-inspection-platform-iot/src/main/java/com/xmkj/business/iot/handler/CommandReplyHandler.java:15`、`:32` | 当前仅记录日志，不更新命令状态或通知前端。 | 新机器狗命令回执 Handler 需按 `requestId` 更新命令流水、推命令结果 WS；同事必须回传 requestId。 |
| OSD/业务状态 WS 参考 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionDeviceStatusBusinessServiceImpl.java:859` 的 `pushOsd`；`MonitorBusinessStatusNotifier.java:31` | 当前 BizCode 和 payload 围绕机场/无人机 OSD。 | 新机器狗 `bizCode`/payload，至少携带 `deviceSn`、`projectId`、上报时间、机器狗 OSD、独立 `businessStatus`。 |
| 监控 REST 入口 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/controller/admin/monitor/controller/FlightMonitorController.java:83`、`:91`、`:108` | `/v2/list`、`/v2/location/list`、`/business-overview` 已有，但当前 service 只组装机场/无人机/相机。 | 在对应 Monitor Service 中按机器狗产品分类增加查询、最近位置和独立状态统计；REST 是 WS 断线/首屏兜底。 |

### 5.3 下行服务、控制权与遥控入口

| 关注点 | 当前代码入口 | 当前行为 | 机器狗改造落点 |
|---|---|---|---|
| P1 调 P2 服务的统一实现 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionIotDeviceServiceImpl.java:81`、`:135` | `invokeDeviceControl(DeviceControlCommand)` 固定使用 `inspection_device_control`，最终调用 `IoTDeviceApi.invokeDeviceService`。 | 复用 RPC 组装/调用逻辑，但该接口参数类型是 DJI 倾向的 `DeviceControlCommand`；新增机器狗 DTO 时，应抽取通用 invoke 能力或新增机器狗专用调用方法，避免把机器狗字段混入 DJI DTO。 |
| 下行 identifier 常量 | `b-inspection-platform-common/src/main/java/com/xmkj/business/common/iot/model/InspectionIotIdentifiers.java:39` | `DEVICE_CONTROL` 当前值正是 `inspection_device_control`。 | 与同事确认机器狗产品 TSL 的同名服务已导入、发布；P1 才可复用该 identifier。 |
| DJI 命令门面参考 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/service/iot/impl/InspectionIotCommandGatewayImpl.java:83`；`:617` | 已把页面/业务命令转成 P2 服务调用，但 `joystickControl` 等逻辑是 DJI DRC。 | 新建机器狗控制门面；低频命令复用其权限/日志/错误映射风格，摇杆不要复用 DRC 坐标和会话逻辑。 |
| 前端无人机摇杆入口 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/controller/admin/control/controller/DrcCommandController.java:57` | `POST /drone/drc/joystick` 使用无人机 `x/y/h/w` 参数和 DRC 权限。 | 新建机器狗控制入口与 `lx/ly/rx/ry/bodyTurn/bodyHighLow` DTO；避免改坏 DJI 接口。 |
| 前端无人机控制权入口 | `b-inspection-platform-core/src/main/java/com/xmkj/business/core/controller/admin/control/controller/DockController.java:222` | `POST /drone/control/actions/authority/flight` 使用 `DroneAuthorityEnum.FLIGHT`。 | 可复用鉴权、审计、获取/释放入口模式；机器狗增加独立会话和抢占规则，不能只塞一个设备类型 if。 |

**实现顺序建议：**先做上行状态 Handler 并用同事给出的样本跑通；之后再做 `RobotDogControlCommand` 的 P2 调用封装。只有在同事确认异步命令回执以及 50Hz 的适配器责任后，才接手动遥控、急停和控制结果 WS。

## 6. 与同事开会时必须当场冻结的清单

按此顺序逐项确认，输出后即可并行开发。

| 编号 | 必须拿到的结论/产物 | 为什么必须今天确认 | 负责人 |
|---|---|---|---|
| B-01 | 已导入的机器狗产品 ID、产品分类编码、`productKey=robot-dog`、测试设备 `deviceId/SN`、网关 ID 的映射表 | P1 不可仅靠 payload 判断设备类型；没有映射无法绑定项目、权限与路由。 | 同事提供，双方核验 |
| B-02 | 状态属性的最终 `params.identifier` 值与完整 `thing.property.post` JSON 样本 | P1 Router 只按这个值选 Handler；当前物模型的 `identifier` 属性尚未给出最终路由值。 | 同事 |
| B-03 | 故障、控制权、命令回执的最终 identifier 与 event JSON 样本 | 当前两条事件与 DJI Handler 同名且不兼容；必须在发布前消除碰撞。 | 同事 + 本人 |
| B-04 | P2 Data Rule：匹配条件、RocketMQ Topic/Tag、投递哪些 identifier、是否过滤高频属性 | 防止消息没有转到 P1 或将 50Hz 原始传感器大量灌入 P1。 | 同事 |
| B-05 | 机器狗业务状态规则输入与优先级 | P1 负责实现独立 `businessStatus`，同事需提供 `motionStatus`、`robotMode`、故障/急停、控制源、在线的确切枚举与时序。 | 双方 + 产品 |
| B-06 | 地图坐标协议 | 物模型目前无经纬度，仅有本地 `motionPositionX/Y/Z`；前端地图需求无法从现有 TSL闭环。 | 同事 + 本人 + 产品 |
| B-07 | 每个 `inspection_device_control.command` 对应协议、ACK、执行完成证据、错误码 | 该服务为 async；P1 不能把“MQ 发送成功”显示为“动作完成”。 | 同事 |
| B-08 | 50Hz 手动控制责任、TTL 和失控保护 | 安全和性能关键；必须由持有 SDK 连接的一侧持续下发并归零。 | 同事主责，本人配合 |
| B-09 | 急停/解除急停状态与权限矩阵 | 软/硬急停、解除前置和外部接管影响前端可操作状态。 | 双方 + 产品 |
| B-10 | RTSP 前/后通道、鉴权、可用性和算法流启停契约 | 视频不走普通物模型状态链路；决定 C-02 的改造范围。 | 同事 + 视频/算法负责人 |

## 7. 当前物模型的阻塞项与建议处理

| 问题 | 证据 | 建议处理 | 是否阻塞 |
|---|---|---|---|
| 状态路由 identifier 未冻结 | 物模型定义属性名 `identifier`，但未固定其状态上报取值 | 冻结为 `inspection_robot_dog_status_report`，并提供属性报文样本。 | 阻塞 P1 状态 Handler |
| 事件 identifier 撞车 | 物模型使用 `inspection_alarm_report`、`inspection_control_notification_report`；P1 现有 Handler 为 DJI payload | 发布前改为机器狗独立 identifier；若不改，必须完成统一 payload 与按设备类型分流改造。 | 阻塞故障/控制权消费 |
| 未定义异步命令回执事件 | 下行两个服务均为 `async`，现有 P1 `CommandReplyHandler` 只记日志 | 新增机器狗命令回执事件和完整关联字段。 | 阻塞控制结果闭环 |
| 地图没有经纬度 | 物模型只有 `motionPositionX/Y/Z` | 决定由适配器转 WGS84，或由 P1 用项目中心/标定矩阵转换；补 frame/mapId/时间/精度契约。 | 阻塞地图 M-02 |
| 高频属性范围过大 | 物模型含 IMU、JointState、运控等高频数据 | Data Rule 仅放行业务 OSD；高频数据另设采样、存储和消费方案。 | 阻塞容量/稳定性验收 |
| 设备上下文仍有机场假设 | 当前 P1 `InspectionDeviceContextService` 以机场/无人机父子关系解析 | P1 新建机器狗独立设备解析与产品分类校验，不复用旧上下文服务。 | 阻塞 P1 Handler 实现 |

## 8. 建议的联调顺序与验收样本

1. 同事先提供 B-01～B-04 的配置截图/导出和脱敏 JSON 样本；P1 以固定 `deviceId` 和 `deviceSn` 写 Handler 单测。
2. 先打通一条状态属性：状态消息进入 P1 后可识别机器狗、写快照、推 OSD 与独立 `businessStatus`。
3. 再打通一条故障“出现—重复—升级—恢复”事件，验证 HMS 数据和 WS。
4. 再打通控制权通知和一个低频动作（如站立），验证异步命令的“发送—受理/完成—OSD 状态变化”三段语义。
5. 最后打通手动控制、急停/解除急停、RTSP 与地图坐标；这些必须实机演练失败路径和断连保护。

每一步至少保留：P2 Data Rule 命中日志、RocketMQ 原消息、P1 Handler 日志、REST/WS 脱敏结果及设备实际状态证据。没有最后一项时，不能将异步命令标为“执行完成”。
