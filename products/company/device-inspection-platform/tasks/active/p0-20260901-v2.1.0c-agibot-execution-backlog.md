# 2.1.0.c 智元机器狗：可执行待办与实施门禁

> 状态：进行中；当前只完成 P1 现状核对与方案拆分，尚未实施代码改造。
>
> 更新日期：2026-09-01
>
> 上游依据：[后端与协议适配对接方案](p0-20260901-v2.1.0c-agibot-backend-adapter-alignment.md)｜[前端接口评审台账](p0-20260901-v2.1.0c-agibot-frontend-interface-review.md)
> 使用方式：先关闭 A 类“外部契约待办”，再按 B 类 P1 任务顺序执行。标为“阻塞”的项未关闭前，不承诺对应 P1 功能完成。
>
> **2026-09-01 复用方案更新：** 同事已明确复用既有 identifier、Handler 与统一控制/配置 DTO；不新增机器狗命令回执事件。涉及上行 identifier、Handler 和回执的本文件旧表述，以[统一 identifier 复用方案评审与待确认清单](p0-20260901-v2.1.0c-agibot-reuse-contract-review.md)为准。
>
> **本轮状态回填：** A-02 的状态上行协议已具备文档级开发输入；A-03、A-07、A-09 已明确一部分协议事实，但仍有业务规则待冻结。其余 A 项不能标为关闭。这里的“已确认”不等同于 P1 已开发或已完成实机联调。

## 1. 任务边界与执行原则

本待办只覆盖巡检业务平台对机器狗的上行消费、状态/告警/控制权业务处理、前端 REST/WS 以及下行控制门面。物联网平台、网关、协议适配器、SDK 映射和设备连接由后端同事负责；P1 不替其实现协议编解码、SDK 连接或 50Hz 持续下发。

| 标记 | 含义 |
|---|---|
| 外部待对齐 | 需要你与后端同事/产品/前端共同冻结的契约或运行前置；我不能替代确认。 |
| P1 待实施 | 契约冻结后，可由我在 `c-drone-inspection` 实施的代码任务。 |
| 阻塞 | 缺少该项时，后续任务不能进入联调或不能声称功能完成。 |
| 可并行 | 前置条件满足后，可与同阶段其他任务同时开发。 |

**总门禁：** 所有样本均须脱敏；HTTP 成功、P2 服务已受理、适配器 ACK、设备状态已变化是四种不同语义，前端显示必须按冻结后的语义处理。

## 2. A 类：你需要与后端同事对齐并取得的产物

这些是 P1 开发输入，不是“口头确认即可”。每项至少应在群/评审记录中留下最终值和一个脱敏样本；“已确认”表示当前同事文档已经给出可用于开发的契约，不替代后续 MQ/实机联调验收。

| 编号 | 当前状态 | 对齐事项与必须取得的产物 | 负责人 | 阻塞的 P1 任务 | 完成判据 |
|---|---|---|---|---|---|
| A-01 | 待确认 | **设备主数据与分类。** 机器狗产品 ID、固定 `categoryCode`、`productKey=robot-dog`、至少一台测试设备的 `deviceId + deviceSn + gatewayId`、项目/空间绑定关系。 | 后端同事提供；你核验 | B-01、B-02、B-03、B-04、B-05 | P2 可按 `deviceId` 查询并返回机器狗 `categoryCode`；测试设备已经绑定 P1 项目/空间。 |
| A-02 | 已确认（文档级） | **机器狗状态上行契约。** 已确认复用 `inspection_device_status_report`，并给出 `thing.property.post` 包装、字段类型、增量规则和示例；P1 实施时先扩展统一最大 `DeviceStatusProperty`，再转机器狗内部 DTO。 | 后端同事；你与我共同核对 | B-02、B-03、B-04 | 文档样本可被 P1 `InspectionIotMessageConverter#convertAll` 解析；实际 MQ 联调时验证机器狗字段不会在统一 `DeviceStatusProperty` 转换时丢失。 |
| A-03 | 部分确认 | **统一事件兼容。** 已确认复用 `inspection_alarm_report`、`inspection_control_notification_report`，并明确 `faults[]`、`CONTROL_TAKEN/RELEASED` 及不新增命令回执事件；仍须冻结故障恢复信号和统一 Handler 内的分类分支位置。 | 后端同事 + 你 | B-05、B-06、B-08 | 现有 Handler 可解析机器狗事件，统一业务 Service 能按设备分类进入机器狗分支；故障恢复信号已定义。 |
| A-04 | 待确认 | **P2 Data Rule 投递。** 匹配条件、RocketMQ Topic/Tag、状态/故障/控制权消息白名单，以及 `1100`–`1104` 高频属性是否转给 P1、最大频率和失败责任；每类消息各给一条 MQ 样本。 | 后端同事 | B-02、B-05、B-06、B-08 | P1 `InspectionIotUpstreamConsumer#handleMessage` 能收到约定消息；高频原始传感器的转发策略已量化，而非默认全量灌入。 |
| A-05 | 待确认 | **业务状态归约规则。** 在线、离线、空闲、工作中、异常的输入字段、优先级、超时阈值和状态变化时机。 | 你 + 产品；同事说明设备字段 | B-03、B-04 | 一张状态决策表：任一组 OSD/故障/控制源输入能推出唯一 `businessStatus`。 |
| A-06 | 待确认 | **地图位置语义。** 经度纬度的来源、坐标系、位置时间、精度、无效/离线展示规则。当前 TSL 只有 `motionPositionX/Y/Z`，不能默认当经纬度。 | 同事 + 你 + 产品 | B-04 | 冻结“适配器输出 WGS84”或“P1 用项目标定参数转换”之一，并提供一条可上图的样本。 |
| A-07 | 部分确认 | **下行服务与状态确认。** 已确认复用 `inspection_device_control`、`inspection_config_command`，服务为异步受理，最终状态来自后续 OSD/控制权事件；仍须冻结命令与页面能力矩阵、允许模式、状态字段和页面超时语义。 | 后端同事；你核对页面语义 | B-07、B-08 | 命令矩阵覆盖本期页面；P2 受理与设备完成分离，完成状态以约定 OSD/控制权事件确认，不要求命令回执。 |
| A-08 | 待确认 | **手动遥控安全边界。** 同事文档规定适配器“一次调用只发一帧，不托管频率/归零”；须明确 P1/前端的 50Hz 发起方式、限流、TTL、浏览器断线/外部抢占/连接丢失后的归零策略。 | 你 + 前端 + 后端同事 | B-08 | 明确每一方的持续发包与归零责任，并有自动归零的实机/模拟证据。 |
| A-09 | 部分确认 | **急停与控制权。** 已知急停/解除急停可走统一控制服务，控制权事件只有 `CONTROL_TAKEN/RELEASED`；仍须补齐获取/释放命令、软硬急停规则、权限矩阵、外部占用和审计要求。 | 你 + 产品 + 同事 | B-06、B-07、B-08 | 急停与恢复互斥规则、控制权状态机和前端可操作条件均书面冻结。 |
| A-10 | 待确认 | **视频边界。** 前后 RTSP 通道、拉流地址归属与鉴权、启停接口、可用性事件和算法流入口是否复用。 | 同事 + 视频/算法负责人 + 前端 | B-09 | 两个通道可通过测试设备验证；明确 P1 是否仅做业务编排，及 WS 是否需要转推视频状态。 |

## 3. B 类：后续由我实施的 P1 代码任务

### 3.1 最小可联调闭环（优先级最高）

| 编号 | 任务与改动点 | 主要代码入口/新增位置 | 前置 | 完成判据 |
|---|---|---|---|---|
| B-01 | **启用机器狗设备分类与投影识别。** 将 P2 冻结的分类编码映射为 P1 `ROBOT_DOG`；保证设备主数据投影不再跳过机器狗。 | 改 `b-inspection-platform-common/.../enums/dict/DictMonitorDeviceCategoryEnum.java`；核对 `b-inspection-platform-core/.../service/device/DeviceProjectionSyncTask.java`、`DeviceProjectionRealtimeSyncService.java`。 | A-01 | 测试设备可进入 P1 设备投影，且监控查询可按机器狗分类取到。 |
| B-02 | **接入机器狗状态消息。** 复用 `inspection_device_status_report`、`DeviceStatusPropertyHandler`；将机器狗字段补入统一最大 `DeviceStatusProperty`，在统一入口按机器狗分类分支，再转换为 P1 内部 `RobotDogStatusProperty`。 | 改 `b-inspection-platform-common/.../iot/model/upstream/DeviceStatusProperty.java`；改 `DeviceStatusPropertyHandler` 或 `InspectionDeviceStatusBusinessServiceImpl` 的分类分派；新增内部机器狗 DTO/业务服务。首选断点：`InspectionIotUpstreamConsumer#handleMessage`、`InspectionIotMessageRouter#route`。 | A-01、A-02、A-04、Q-06 | 给定状态 MQ 样本，仍命中现有状态 Handler，机器狗字段未丢失且进入机器狗分支；无人机分支回归不受影响。 |
| B-03 | **机器狗独立设备上下文与增量状态快照。** 用 `deviceId + deviceSn + categoryCode + 项目归属` 校验；对机器狗增量属性按冻结时序规则合并，再留存页面所需状态和上报时间。 | 新增 `RobotDogDeviceContextService`、`RobotDogStatusBusinessService`；查询实现参考 `ManagedDeviceQueryService#getDeviceById`、`IotDeviceQueryGateway#mapToDeviceDTO`；状态分派入口为 `InspectionDeviceStatusBusinessServiceImpl#handle`。 | A-01、A-02、A-05、Q-04、Q-05 | 非机器狗或 `deviceId/SN` 不一致消息被拒绝；缺失字段不清空旧值；乱序和过期行为符合冻结规则。 |
| B-04 | **监控列表、总览、位置 REST 兼容机器狗。** 给现有监控 service 增加机器狗独立查询分支、总览数量、业务状态统计和最近位置。 | 入口 `FlightMonitorController`：`/drone/monitor/v2/list`、`/v2/location/list`、`/overview`、`/business-overview`；实现参考 `MonitorDeviceServiceImpl`。 | B-01、B-03；地图还需 A-06 | 列表可按 `ROBOT_DOG` 筛选；总览/业务统计包含机器狗；位置按冻结坐标规则返回。 |
| B-05 | **机器狗 OSD 与监控 WS。** 新增机器狗状态/业务状态推送 payload 和 `bizCode`，消息带 `projectId`、设备 ID/SN、上报时间、版本或增量标识。 | 参考 `InspectionDeviceStatusBusinessServiceImpl#pushOsd`、`MonitorBusinessStatusNotifier`；新增机器狗 WS DTO/Notifier。 | B-03、前端订阅字段冻结 | 前端连接后收到首个状态更新；断线时 REST 可回补；不复用 DJI `device_osd` payload。 |

### 3.2 告警、控制权与下行控制

| 编号 | 任务与改动点 | 主要代码入口/新增位置 | 前置 | 完成判据 |
|---|---|---|---|---|
| B-06 | **机器狗故障闭环。** 复用 `AlarmReportHandler`，扩展统一 `AlarmReport` 承接 `faults[]`，在 `InspectionAlarmBusinessServiceImpl` 内按机器狗分类分支，定义唯一键、合并、恢复、持续时长。 | 改 `AlarmReport`、`InspectionAlarmBusinessServiceImpl`；复用 `DeviceHmsController` 的 `/drone/hms/page` 前先核对字段兼容性。 | A-03、A-04、Q-07、Q-08 | 故障出现、重复、升级、恢复均符合已冻结的可信信号；24 小时列表返回机器狗文案和持续时间。 |
| B-07 | **机器狗控制权与急停业务。** 复用 `ControlNotificationReportHandler` 和统一控制通知服务，支持 `CONTROL_TAKEN/RELEASED` 的机器狗上下文；急停/恢复用统一 `inspection_device_control` 命令和 OSD 状态确认。 | 改 `InspectionControlNotificationBusinessServiceImpl`；参照 `DockController` 的权限/审计模式，但不改 DJI DRC 枚举与会话。 | A-03、A-09、Q-09、Q-12 | 外部占用变化可实时通知；只有在 Q-09 定义了获取/释放命令后，才实现完整控制权状态机。 |
| B-08 | **机器狗下行控制与状态确认。** 扩展统一 `DeviceControlCommand`、`ConfigCommand`，机器狗调用显式使用 `ASYNC`；复用 P1→P2 RPC 基础设施，并用 OSD/控制权事件而非回执 Handler 更新页面。 | 改 `DeviceControlCommand`、`ConfigCommand`、`InspectionIotDeviceServiceImpl`；机器狗业务门面参考 `InspectionIotCommandGatewayImpl`。 | A-03、A-07、A-08、A-09、Q-10、Q-11、Q-12 | 每个低频命令传入正确 TSL 参数；HTTP 仅表示受理；最终 UI 由冻结状态字段或超时规则更新；遥控责任符合 Q-10。 |
| B-09 | **双路视频业务接入。** 依据 A-10 确定复用/改造直播链路或新增机器狗视频门面，并处理可用性提示。 | 参考 `/drone/live/stream/*`、算法流启动入口；具体改动以 A-10 契约为准。 | A-10 | 前/后视频可独立打开、关闭、鉴权；异常时前端收到冻结的提示语义。 |

### 3.3 接口、自动化验证与交接

| 编号 | 任务与改动点 | 前置 | 完成判据 |
|---|---|---|---|
| B-10 | 补齐 REST/WS DTO、OpenAPI 注解、错误语义、权限点及接口台账字段；同步维护前端评审文档中的负责人和联调状态。 | 对应功能的 A/B 任务完成接口字段冻结 | 每个接口有请求、响应、失败语义；WS 有订阅范围、首屏/增量/兜底规则。 |
| B-11 | 编写单元/集成测试：Converter/Router 路由、机器狗分类拒绝、状态归约、告警合并恢复、控制权状态机、命令受理与状态确认。 | B-01 至 B-08 分阶段完成 | 核心输入样本可自动验证；回归测试证明 DJI 上行/下行不受影响。 |
| B-12 | 联调验收与发布交接：记录脱敏 MQ 样本、P2 调用样本、页面录屏、错误码表、已知限制和回滚开关/路径。 | B-04 至 B-11 | 每项前端原型能力有对应验收证据；未做项明确标注“本期不做”。 |

## 4. 推荐执行顺序

```text
第 0 步：关闭 A-01、A-03、A-04（A-02 协议已确认；仍在 B-02 的 MQ 联调验收）
  ↓
第 1 步：B-01 → B-02 → B-03（机器狗可被识别、消费、存状态）
  ↓
第 2 步：A-05 / A-06 冻结后，B-04 与 B-05 并行（监控 REST + WS）
  ↓
第 3 步：A-03 / A-07 / A-09 冻结后，B-06、B-07、B-08 并行
  ↓
第 4 步：A-10 冻结后 B-09；全过程同步 B-10，最后 B-11 → B-12
```

最小演示里程碑是 **B-03**：测试机器狗状态消息从 P2 进入 P1，路由到既有状态 Handler 后进入机器狗业务分支，完成设备分类校验并形成可查询状态快照。未达到该里程碑前，不开始承诺驾驶舱、控制或地图页面的交付时间。

## 5. 当前仍需收口的问题

详细问题、代码事实和建议负责人见[统一 identifier 复用方案评审与待确认清单](p0-20260901-v2.1.0c-agibot-reuse-contract-review.md)。本轮开会只需优先收口以下事项：

1. **A-01 / Q-01：** 测试机器狗的 `deviceId`、SN、`categoryCode`、项目/空间绑定，以及设备分类不匹配时以谁为准。
2. **A-03 / Q-07：** `faults=[]` 不能表示恢复；故障恢复的真实信号、唯一键和合并窗口是什么。
3. **A-04 / Q-03：** Data Rule 的 Topic/Tag、投递白名单与 `1100`–`1104` 高频数据的最大频率、失败责任。
4. **A-05 / Q-04、Q-05：** 增量状态合并、乱序/空值/TTL，以及 `reportTime` 的长整型序列化类型。
5. **A-06：** 地图经纬度、坐标系和离线展示，不能由本地 `motionPositionX/Y/Z` 推断。
6. **A-07 / Q-11、Q-12：** 命令参数是否允许 P1 额外透传 `targetDeviceSn`，以及无命令回执下各页面的成功/失败/超时呈现。
7. **A-08 / Q-10：** 50Hz 遥控的定时发送、限流、TTL 和所有断开场景的自动归零责任。
8. **A-09 / Q-09：** 完整控制权生命周期（获取、释放、抢占、外部占用）及急停互斥和权限规则。
9. **A-10 / Q-14：** 双路 RTSP、鉴权、视频可用性及 P1 前端接口范围。

## 6. 当前不能开始实施的事项

| 事项 | 原因 | 解除条件 |
|---|---|---|
| 机器狗状态上行消费 | A-02 的 identifier 与包装契约已确认，但 P1 尚未扩展最大 DTO、分类分支和 Data Rule 投递。 | B-02 实施；A-01、A-04 关闭。 |
| 告警/控制权 Handler | 已确认复用现有 identifier/Handler 且无命令回执事件；故障恢复、分类分支和 Data Rule 未冻结。 | A-03、A-04 关闭。 |
| 地图位置接口 | 当前 TSL 无经纬度，不能把本地 `motionPositionX/Y/Z` 当地理坐标。 | A-06 关闭。 |
| 遥控、急停与控制结果页面 | 下行服务是 async，且 50Hz、ACK、控制权、急停安全规则未冻结。 | A-07、A-08、A-09 关闭。 |
| 双路视频 | 当前机器狗 RTSP 与现有无人机直播链路的复用范围未确认。 | A-10 关闭。 |

## 7. 证据与后续维护

- P1 当前代码入口与已验证事实见[后端与协议适配对接方案](p0-20260901-v2.1.0c-agibot-backend-adapter-alignment.md)。
- 前端接口、WS 类别和本期范围见[前端接口评审台账](p0-20260901-v2.1.0c-agibot-frontend-interface-review.md)。
- 每关闭一个 A 类任务，应把最终契约、样本来源、负责人和日期回填到本文件；如契约改变现有假设，也同步更新上述对接方案。
- 我收到你明确授权后，按 B 类编号逐项实施、运行对应验证，并在本文件回填“已完成/证据/遗留风险”。
