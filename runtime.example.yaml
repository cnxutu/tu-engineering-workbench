# Local-only Runtime Target binding template. Copy to runtime.local.yaml.
# Keep Host, User, and IdentityFile exclusively in ~/.ssh/config.
version: v1
environments:
  company-dev:
    targets:
      - shortcut: "<local-runtime-shortcut>"
        ssh_alias: "<local-ssh-alias>"
